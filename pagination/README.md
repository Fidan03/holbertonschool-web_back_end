# Pagination

This project covers pagination techniques for Python back-end applications.

## Tasks

### 0. Simple helper function
`index_range(page, page_size)` returns a tuple `(start_index, end_index)` for the given 1-indexed page and page size.
